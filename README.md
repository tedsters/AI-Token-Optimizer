# AI-Token-Optimizer
 definitive runbook for setting up, running, and understanding the Local AI Optimization Showcase. Every step runs entirely on your machine: generation is served by a local Ollama instance, and no document content leaves the host.
